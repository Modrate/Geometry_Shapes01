public class Rectangle {

    double length;
    double breadth;

    double area (){

        double a = length*breadth;
        return a;
    }

    double parameter(){
        double p = 2*length*breadth;
        return p;
    }
}
